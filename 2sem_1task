#include <stdio.h>

int main(void)
{
    printf("Enter the string: ");
    int a;
    a = getchar();
    while (a != '\n')
    {
        if (a!=' ' && a!='\t')
        {
            putchar(a);
        }
        a = getchar();
    }
    return 0;
}

/*  получаю строку символов, создаю цикл в котором перезаписываю строку(если таб или пробел иду дальше, если символ то перезаписываю,
таким образом оставля в строке только символы)  */
